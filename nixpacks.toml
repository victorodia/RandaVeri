[phases.setup]
nixPkgs = ["python311", "libjpeg", "zlib", "libpng", "libwebp"]

[phases.build]
cmds = ["python3 -m pip install -r backend/requirements.txt"]

[start]
cmd = "cd backend && python3 -m gunicorn main:app -w 1 -k uvicorn.workers.UvicornWorker --bind 0.0.0.0:$PORT"
